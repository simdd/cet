#if 0
	shc Version 4.0.1, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f cet.sh -o cet 
#endif

static  char data [] = 
#define      pswd_z	256
#define      pswd	((&data[12]))
	"\117\336\312\224\043\316\044\264\130\066\267\077\242\014\071\335"
	"\063\226\215\267\266\302\007\003\046\270\251\214\032\127\102\174"
	"\137\171\061\337\015\257\026\330\061\212\116\060\030\122\077\063"
	"\262\107\317\271\271\335\207\325\327\130\342\245\150\011\032\336"
	"\025\256\315\344\062\315\065\302\331\032\205\241\216\243\054\355"
	"\261\151\075\120\233\152\022\073\024\065\035\126\157\275\072\357"
	"\236\035\033\004\265\130\040\110\374\010\053\351\230\352\371\122"
	"\304\246\003\047\243\072\116\104\327\137\133\004\302\317\126\260"
	"\312\100\315\213\045\165\020\372\371\263\023\205\344\302\241\164"
	"\014\040\006\350\050\044\161\120\275\126\207\105\362\030\071\376"
	"\334\005\243\030\360\256\376\056\335\234\304\226\213\224\361\334"
	"\164\001\064\146\140\222\225\051\276\004\253\320\373\144\252\360"
	"\112\126\053\341\214\322\063\304\257\210\240\330\305\055\043\200"
	"\115\042\065\362\265\047\311\250\260\045\052\134\154\125\355\367"
	"\311\160\261\205\150\362\372\103\311\346\356\131\001\010\365\256"
	"\253\203\226\323\173\050\154\002\277\306\252\333\222\043\071\076"
	"\176\153\134\350\300\323\104\164\062\305\302\200\135\370\255\324"
	"\114\170\251\342\206\213\024\336\277\227\165\241\013\227\045\163"
	"\075\027\031\112\047\127\064\363\251\071\244\023\346\120\156\355"
	"\166\320\203\100"
#define      date_z	1
#define      date	((&data[308]))
	"\364"
#define      msg1_z	65
#define      msg1	((&data[310]))
	"\207\245\045\030\137\247\372\376\331\143\327\242\222\144\366\353"
	"\266\324\121\233\271\203\134\276\003\016\202\367\022\131\251\321"
	"\140\146\066\332\126\147\107\132\302\026\055\065\346\176\361\011"
	"\254\206\140\121\043\277\070\320\206\123\242\261\203\114\044\026"
	"\354\213\254"
#define      chk2_z	19
#define      chk2	((&data[377]))
	"\063\232\341\364\203\374\267\142\253\113\076\274\026\011\055\100"
	"\257\277\215\134"
#define      msg2_z	19
#define      msg2	((&data[397]))
	"\376\162\330\362\076\024\251\063\202\247\341\037\252\164\035\256"
	"\205\013\155\132\056\073"
#define      xecc_z	15
#define      xecc	((&data[421]))
	"\353\065\062\315\013\305\033\003\316\042\272\003\307\077\303\177"
	"\200\266\375\221\327"
#define      lsto_z	1
#define      lsto	((&data[439]))
	"\275"
#define      chk1_z	22
#define      chk1	((&data[444]))
	"\254\312\327\337\143\373\136\025\076\147\217\307\136\377\132\133"
	"\223\376\301\171\311\376\340\331\305\212"
#define      text_z	626
#define      text	((&data[552]))
	"\122\222\045\246\263\134\217\032\073\375\073\325\161\372\166\006"
	"\024\301\277\020\060\016\261\120\207\064\244\255\273\263\302\376"
	"\342\056\046\236\245\045\325\307\204\054\341\172\272\064\337\203"
	"\134\222\114\251\224\130\245\170\365\110\226\356\050\256\276\260"
	"\125\211\127\220\255\015\376\102\141\035\370\270\152\160\021\027"
	"\153\055\124\360\305\123\324\155\120\175\003\036\046\243\114\225"
	"\131\043\233\340\012\304\036\173\337\157\241\224\311\020\073\000"
	"\323\320\271\027\214\357\037\265\210\141\070\210\140\327\200\140"
	"\076\377\271\031\152\033\375\272\054\000\027\027\302\154\166\054"
	"\301\127\357\216\377\311\140\145\133\030\374\026\370\233\202\205"
	"\267\225\364\344\245\274\373\133\377\241\004\255\142\253\351\156"
	"\151\022\267\306\013\224\166\174\330\263\044\213\061\011\101\230"
	"\306\300\073\003\012\105\367\027\172\042\101\007\274\123\220\120"
	"\011\344\023\117\072\223\005\101\170\330\252\155\117\245\352\052"
	"\354\277\224\375\135\252\343\011\054\137\107\161\137\036\347\156"
	"\046\165\076\005\134\216\314\260\124\124\025\321\032\373\021\221"
	"\046\337\360\004\230\016\377\323\175\163\135\077\000\264\363\224"
	"\327\011\062\347\205\076\247\374\377\060\263\146\374\175\106\312"
	"\304\317\252\273\121\306\140\173\302\026\026\103\066\235\064\060"
	"\022\322\215\151\271\127\145\136\222\301\112\214\075\156\022\112"
	"\276\025\216\157\317\126\202\366\102\170\142\313\244\107\235\332"
	"\072\116\121\247\213\176\044\211\145\311\025\213\154\260\067\043"
	"\205\224\115\306\020\372\323\043\064\247\121\105\256\163\217\141"
	"\062\177\162\213\052\042\312\113\206\350\065\275\211\230\305\326"
	"\312\053\253\150\253\352\116\342\077\055\203\124\267\253\102\231"
	"\212\322\000\113\167\076\005\145\151\157\034\214\350\150\315\262"
	"\045\206\320\054\064\201\336\016\034\176\046\077\264\142\053\123"
	"\347\124\263\254\020\016\262\022\315\306\174\230\022\041\022\126"
	"\052\163\167\161\012\064\101\077\100\035\374\071\317\354\164\110"
	"\342\156\003\342\072\024\340\071\150\134\234\247\320\046\153\037"
	"\323\112\266\317\036\064\270\277\023\353\031\124\263\241\125\215"
	"\324\277\003\034\235\115\146\135\174\067\103\256\275\162\336\364"
	"\234\332\254\237\006\244\130\332\017\034\123\051\065\246\021\127"
	"\351\214\017\054\163\050\166\156\117\047\151\346\037\310\374\147"
	"\325\347\047\350\366\243\016\047\262\334\321\256\266\304\272\123"
	"\235\027\113\166\145\141\063\176\242\327\365\074\000\233\127\326"
	"\220\226\001\315\347\105\110\045\325\075\025\057\212\124\126\336"
	"\053\131\350\215\122\207\161\363\277\340\033\223\032\234\254\273"
	"\356\105\224\327\232\354\007\203\044\300\303\334\235\355\031\220"
	"\255\230\347\004\124\003\121\355\327\305\265\006\364\370\312\301"
	"\205\102\035\116\152\270\047\244\254\041\232\006\261\303\241\343"
	"\213\216\324\203\240\122\175\031\144\237\112\163\175\115\166\211"
	"\004\043\216\271\376\227\146\254\306\204\265\147\034\070\040\271"
	"\145\007\134\143\110\346\324\325\223\025\266\141\252\052\331\063"
	"\371\012\015\322\322\023\162\331\021\340\134\143\043\254\103\076"
	"\273\202\233\052\212\150\027\115\177\205\213\055\035\126\304\271"
	"\325\136\300\027\204\043\267\345\133\133\030\162\234\321\072\352"
#define      tst1_z	22
#define      tst1	((&data[1219]))
	"\337\251\072\325\244\022\225\050\346\031\326\217\216\173\212\142"
	"\305\364\135\366\001\124\146\044\117\357\176\217"
#define      shll_z	10
#define      shll	((&data[1246]))
	"\037\163\161\362\322\001\200\161\220\051"
#define      inlo_z	3
#define      inlo	((&data[1256]))
	"\326\146\245"
#define      rlax_z	1
#define      rlax	((&data[1259]))
	"\022"
#define      opts_z	1
#define      opts	((&data[1260]))
	"\216"
#define      tst2_z	19
#define      tst2	((&data[1264]))
	"\265\305\136\056\142\167\313\342\020\227\222\310\330\347\356\254"
	"\300\314\123\353\342\230"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define HARDENINGSP	0	/* Define as 1 to disable bash child process */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a processs */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;

    int lentmp = len;

#if HARDENINGSP
    //Start tracing to protect from dump & trace
    if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }

    //Decode Bash
    while (len > 0) {
        indx++;
        tmp = stte[indx];
        jndx += tmp;
        stte[indx] = stte[jndx];
        stte[jndx] = tmp;
        tmp += stte[indx];
        *ptr ^= stte[tmp];
        ptr++;
        len--;
    }

    //Exec bash script
    system(tmp2);

    //Empty script variable
    memcpy(tmp2, str, lentmp);

    //Sinal to detach ptrace
    ptrace(PTRACE_DETACH, 0, 0, 0);
    exit(0);

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
#endif /* HARDENINGSP Exit here anyway*/

    int pid, status;
    pid = fork();

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            printf("Operation not permitted\n");
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Exec bash script
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {
        wait(&status);
    }

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
} 
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
